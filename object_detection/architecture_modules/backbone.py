# Definition of the backbone, multiple convolution blocks.
import torch
from torch import nn
from typing import Union, Tuple, Optional


class Backbone(nn.Module):
    def __init__(self, ):
        super(Backbone, self).__init__()
